---
interface Props {
  quote: string;
  attribution: string;
}

const { quote, attribution } = Astro.props;
---

<blockquote class="quote-block">
  <p>"{quote}"</p>
  <cite>{attribution}</cite>
</blockquote>
